@model IReadOnlyCollection<TeamGram.Phrases.ServerIsEmptyPhrase>

<div class="ui container">
    <h2>Teamspeak messages management</h2>

    <table class="ui celled table">
        <thead>
        <tr>
            <th>Text</th>
            <th class="thauto">
                <button class="ui inverted green icon button" type="submit" onclick="openModal()">
                    <i class="plus circle icon"></i>
                </button>
            </th>
        </tr>
        </thead>
        <tbody>
        @foreach (var serverIsEmptyPhrase in Model)
        {
            <tr>
                <td>@serverIsEmptyPhrase.Text</td>
                <td>
                    <form action="/teamspeak/phrase/@serverIsEmptyPhrase.Id/delete" method="post">
                        <input type="hidden" name="pageUrl" value="@Url.Action("EmptyServerPage")"/>
                        <button class="ui inverted red icon button" type="submit" width="">
                            <i class="minus circle icon"></i>
                        </button>
                    </form>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>

<form asp-action="AddEmptyServerMessage" method="post" class="ui modal">
    <i class="close icon"></i>
    <div class="header">
        Add new empty server message
    </div>
    <div class="content">
        <div class="ui form">
            <div class="field">
                <label>Text</label>
                <input type="text" name="text" placeholder="Text" required>
            </div>
        </div>
    </div>
    <div class="actions">
        <button class="ui red negative button">Cancel</button>
        <button class="ui green button" type="submit">Submit</button>
    </div>
</form>

<script>
function openModal()
{
    $('.ui.modal')
      .modal('show')
    ;
}
</script>